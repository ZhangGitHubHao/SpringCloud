server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ZhangGitHubHao/trendConfig
          search-paths: respo
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8889/eureka/

